import { CodexCliProvider } from "@project-relay/providers"; export const dynamic="force-dynamic";
export default async function Page(){const status=await new CodexCliProvider().checkAvailability();return <><div className="eyebrow">Local controls</div><h1>Settings</h1><div className="grid"><section className="card span6"><h2>Codex CLI</h2><div className="row"><span>{status.available?status.version:"Not available"}</span><span className={`pill ${status.available?"good":"warn"}`}>{status.available?"READY":"ACTION REQUIRED"}</span></div><p className="subtle">{status.setup??"ProjectRelay uses the locally authenticated CLI and never asks for or stores your ChatGPT password."}</p></section><section className="card span6"><h2>Execution policy</h2><p className="subtle">One worker job at a time. Structured allowlisted commands only. Destructive categories require recorded approval. Logs redact likely secrets and omit secret-bearing environment variables.</p></section></div></>}
