import{NextResponse}from"next/server";import{providerRegistry}from"@project-relay/providers";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),65_000);try{const result=await providerRegistry.get(id).testConnection(controller.signal);return NextResponse.json(result,{status:result.ok?200:503});}finally{clearTimeout(timer);}}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Provider test failed."},{status:400});}}
