import{NextResponse}from"next/server";import{providerRegistry}from"@project-relay/providers";import{getProviderHealthQueue}from"../../../../../lib/redis";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;providerRegistry.get(id);await getProviderHealthQueue().add("check",{providerId:id as "codex-cli"|"claude-cli",mode:"refresh"},{jobId:`provider-refresh-${id}`,removeOnComplete:20,removeOnFail:20});return NextResponse.json({queued:true});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Provider refresh failed."},{status:400});}}
