import{NextResponse}from"next/server";import{providerRegistry}from"@project-relay/providers";import{apiErrorResponse,ApiError}from"../../../../../lib/api-errors";import{getProviderHealthQueue}from"../../../../../lib/redis";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;try{providerRegistry.get(id);}catch{throw new ApiError("NOT_FOUND",`Unknown provider '${id}'.`);}await getProviderHealthQueue().add("check",{providerId:id as "codex-cli"|"claude-cli",mode:"refresh"},{jobId:`provider-refresh-${id}`,removeOnComplete:20,removeOnFail:20});return NextResponse.json({queued:true});}catch(error){return apiErrorResponse(error,"POST /api/providers/[id]/refresh");}}
