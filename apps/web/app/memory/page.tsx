import { prisma } from "@project-relay/database"; import { readMemory,MEMORY_FILES } from "@project-relay/project-memory"; export const dynamic="force-dynamic";
export default async function Page(){let project;try{project=await prisma.project.findFirst({where:{archivedAt:null},orderBy:{updatedAt:"desc"}});}catch{}const files=project?await Promise.all(MEMORY_FILES.map(async name=>({name,content:await readMemory(project.repositoryPath,name)}))):[];return <><div className="eyebrow">Structured source of truth</div><h1>Project memory</h1><p className="subtle">Updates are append-only and attributed. Existing user-authored content is never silently replaced.</p><div className="grid">{files.map(file=><section className="card span6" key={file.name}><h2>{file.name}</h2><pre className="log">{file.content||"Empty"}</pre></section>)}{!files.length&&<section className="card span12 empty">Register a project to inspect its memory.</section>}</div></>}
