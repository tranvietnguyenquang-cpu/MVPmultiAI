"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{csrfFetch}from"../lib/csrf-client";
export function CheckpointButton({projectId,taskId}:{projectId:string;taskId:string}){const router=useRouter();const[message,setMessage]=useState("");async function create(){setMessage("Creating…");const response=await csrfFetch("/api/checkpoints",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({projectId,taskId,reason:"Manual checkpoint"})});const data=await response.json()as{error?:string};setMessage(response.ok?"Checkpoint created.":data.error??"Failed.");router.refresh();}return <div className="row"><button className="button secondary" onClick={create}>Create checkpoint</button><span className="subtle">{message}</span></div>}
