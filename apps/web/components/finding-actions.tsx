"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{csrfFetch}from"../lib/csrf-client";
export function FindingActions({id,disabled}:{id:string;disabled:boolean}){const router=useRouter();const[busy,setBusy]=useState(false);async function act(action:"ACCEPT"|"REJECT"){setBusy(true);await csrfFetch(`/api/findings/${id}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action})});setBusy(false);router.refresh();}return <span>{disabled?"Stale—rerun review":<><button disabled={busy} onClick={()=>act("ACCEPT")}>Accept & create remediation</button> <button className="button secondary" disabled={busy} onClick={()=>act("REJECT")}>Reject</button></>}</span>}
