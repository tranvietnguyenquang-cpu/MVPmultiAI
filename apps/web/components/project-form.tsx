"use client";import{useRouter}from"next/navigation";import{useState}from"react";import{csrfFetch}from"../lib/csrf-client";
export function ProjectForm(){const router=useRouter();const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const body={name:form.get("name"),repositoryPath:form.get("repositoryPath"),commandIds:["test","typecheck","lint","build"]};const response=await csrfFetch("/api/projects",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const result=await response.json()as{id?:string;error?:string};setBusy(false);if(!response.ok){setError(result.error??"Could not register project.");return;}router.push(`/projects/${result.id!}`);router.refresh();}return <form className="form" onSubmit={submit}><label>Project name<input name="name" required/></label><label>Absolute repository path<input name="repositoryPath" placeholder="C:\\work\\my-project" required/></label><p className="subtle">The server enables its fixed test, typecheck, lint, and build command definitions. Browser input cannot define executables or arguments.</p>{error&&<p className="warn">{error}</p>}<button disabled={busy}>{busy?"Validating…":"Register repository"}</button></form>}
