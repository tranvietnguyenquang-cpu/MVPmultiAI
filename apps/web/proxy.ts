import type{NextRequest}from"next/server";import{NextResponse}from"next/server";import{CSRF_COOKIE,validateMutationRequest}from"./lib/csrf";
export function proxy(request:NextRequest){if(!request.nextUrl.pathname.startsWith("/api/")||["GET","HEAD","OPTIONS"].includes(request.method))return NextResponse.next();const cookieToken=request.cookies.get(CSRF_COOKIE)?.value;const headerToken=request.headers.get("x-csrf-token")??undefined;const allowed=validateMutationRequest({method:request.method,requestOrigin:request.headers.get("origin"),serverOrigin:request.nextUrl.origin,...(cookieToken?{cookieToken}:{}),...(headerToken?{headerToken}:{})});return allowed?NextResponse.next():NextResponse.json({error:"Same-origin CSRF validation failed."},{status:403});}
export const config={matcher:"/api/:path*"};
