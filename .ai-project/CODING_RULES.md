# Coding Rules

- TypeScript strict mode is mandatory.
- Parse every external boundary with Zod or an explicit type guard.
- Never spawn browser-provided shell text.
- Never overwrite project memory; create missing files and append attributable updates.
- Never set `VERIFIED` without successful evidence for every criterion.
- Keep provider details behind `CodingProvider`.
- Treat Git and process results as authoritative.

## Update — 2026-07-22T05:58:48Z
- What changed: Added implementation rules used by capsule generation.
- Why: Make fresh sessions follow stable project constraints.
- Task: bootstrap-mvp
- Agent: codex
- Evidence: packages/shared; packages/execution; packages/providers
- Confidence: HIGH
