import { providerRegistry } from "@project-relay/providers";
async function main(){const id=process.argv[2];if(!id)throw new Error("Provider ID required.");const provider=providerRegistry.get(id);const installation=await provider.refreshHealth();console.log(JSON.stringify({provider:id,installed:installation.installed,version:installation.version,checkedAt:installation.checkedAt}));if(!installation.installed){console.error(provider.setupInstructions);process.exitCode=2;return;}const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),65_000);try{const result=await provider.testConnection(controller.signal);console.log(JSON.stringify({authenticated:result.probe.authentication,ok:result.ok,durationMs:result.durationMs,resetAt:result.probe.resetAt}));if(!result.ok)process.exitCode=1;}finally{clearTimeout(timer);}}
void main().catch(error=>{console.error(error instanceof Error?error.message:String(error));process.exitCode=1;});
